<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.creditscore.sujeet.samplecreditscore">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivityDisp1" />
        <activity android:name=".Login" />
        <activity android:name=".SignUp" />
        <activity android:name=".Profile" />
        <activity android:name=".OtpVerification" />
        <activity
            android:name=".LiveCreditScore"
            android:label="CreditScore" />
        <activity android:name=".Prediction" />
        <activity android:name=".Rental" />
        <activity android:name=".Benifit" />
        <activity android:name=".Faq" />
        <activity android:name=".Notification" />
    </application>

</manifest>